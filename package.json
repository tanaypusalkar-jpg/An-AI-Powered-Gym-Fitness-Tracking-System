{
  "name": "ai-gym-fitness-assistant",
  "private": true,
  "version": "1.0.0",
  "description": "Root-level runner: starts both the FastAPI backend and the Vite frontend with a single command.",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uv run uvicorn main:app --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && uv venv && uv pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
